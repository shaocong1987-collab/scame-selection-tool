version: '3.8'

services:
  chromadb:
    image: chromadb/chroma:latest
    container_name: scame-chromadb
    ports:
      - "8000:8000"
    environment:
      - IS_PERSISTENT=TRUE
      - PERSIST_DIRECTORY=/chroma/chroma_data
      - ANONYMIZED_TELEMETRY=FALSE
    volumes:
      - chroma_data:/chroma/chroma_data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/heartbeat"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  # PostgreSQL数据库（未来使用）
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: scame-postgres
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     POSTGRES_USER: scame
  #     POSTGRES_PASSWORD: scame_password
  #     POSTGRES_DB: scame_selection
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   restart: unless-stopped
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U scame"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 3
  #     start_period: 20s

volumes:
  chroma_data:
  # postgres_data: